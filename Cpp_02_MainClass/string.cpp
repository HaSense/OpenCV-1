#include "opencv2/opencv.hpp"
#include <iostream>

using namespace std;
using namespace cv;

//typedef std::string String;

//String format(const char* fmt, ...);

int main() {
	// test01.bmp, test02.bmp, test3.bmp 불러오기
	/*
	Mat imgs[3];
	for (int i = 0; i < 3; i++) {
		String filename = format("test%02d.bmp", i + 1);
		imgs[i] = imread(filename);
	}
	*/
	
	return 0;
}